import React from 'react';
import '../styles/Portfolio.css';

const Portfolio = () => {
    return (
        <div>
            <p>test test</p>
        </div>
    )
}

export default Portfolio


// have collage of photos that when you hover on them the photo will transition upwords revealing a paragraph on that specific photo's project